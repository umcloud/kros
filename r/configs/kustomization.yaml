# Use kustomize to merge:
# - cluster.yaml (upstream)
# - cluster-tunables.yaml (committed in-tree)
# - cluster-nodes.yaml (autogenerated)
resources:
- cluster.yaml
patches:
- path: cluster-tunables.yaml
  target:
    kind: CephCluster
- path: cluster-nodes.yaml
  target:
    kind: CephCluster
